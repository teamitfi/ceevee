meta {
  name: Profile
  type: http
  seq: 3
}

get {
  url: http://localhost:4000/api/v1/private/profile
  body: none
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Authorization: Bearer {{getVar("token")}}
}

script:pre-request {
  const token = bru.getVar("accessToken");
  req.setHeader("Authorization", `Bearer ${token}`);
}
